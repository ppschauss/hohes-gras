{
  "name": "poke-partner",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "npm run build -w @game/shared && npm run build -w @game/content && npm run build -w @game/engine && npm run build -w @game/api && npm run build -w @game/web",
    "dev:api": "npm run dev -w @game/api",
    "dev:web": "npm run dev -w @game/web",
    "_comment_pretest": "Ohne diesen Schritt testet vitest gegen ein altes dist/: paketuebergreifende Importe kommen aus den gebauten Dateien, nicht aus den Quellen. Ein gruener Lauf sagte dann nichts.",
    "_comment_pretest_web": "Das Web-Paket haengt nicht am Projektverbund von 'tsc -b' — es baut mit Vite und setzt noEmit. Es wurde deshalb bei keinem Testlauf geprueft: eine Zeile im API-Client mit erfundener Funktion UND erfundenem Typ ging glatt durch und fiel erst beim Web-Build auf. Seitdem laeuft die Pruefung hier mit.",
    "pretest": "tsc -b && tsc -p packages/web --noEmit",
    "test": "vitest run",
    "typecheck": "tsc -b --force && tsc -p packages/web --noEmit",
    "world": "node --experimental-strip-types --disable-warning=ExperimentalWarning tools/rebuild-world.ts",
    "import": "node --experimental-strip-types --disable-warning=ExperimentalWarning tools/import-pokeapi.ts",
    "_comment_import": "Der Bereich muss zum Pack passen: ein kleinerer Lauf ueberschreibt es und nimmt Regionen mit. import:full deckt alle drei Regionen ab.",
    "import:kanto": "node --experimental-strip-types --disable-warning=ExperimentalWarning tools/import-pokeapi.ts --dex 1-151",
    "import:johto": "node --experimental-strip-types --disable-warning=ExperimentalWarning tools/import-pokeapi.ts --dex 1-251",
    "import:full": "node --experimental-strip-types --disable-warning=ExperimentalWarning tools/import-pokeapi.ts --dex 1-386",
    "simulate": "node --experimental-strip-types --disable-warning=ExperimentalWarning tools/simulate.ts"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vitest": "^2.1.8",
    "@types/node": "^22.10.2"
  }
}
